<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.pumpgunstudios.amazonmusicrpc.fakeamazon.CONTROL_PLAYBACK" />

    <permission
        android:name="com.pumpgunstudios.amazonmusicrpc.fakeamazon.CONTROL_PLAYBACK"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_fake_amazon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".FakeAmazonMusicService"
            android:exported="true"
            android:permission="com.pumpgunstudios.amazonmusicrpc.fakeamazon.CONTROL_PLAYBACK"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
